import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs';
import { CompactDialog } from '../../components/CompactDialog';

<Meta title="Library/CompactDialog/Documentation" />

# CompactDialog

The `<CompactDialog>` comes with these props:

<ArgsTable of={CompactDialog} />
