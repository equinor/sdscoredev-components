import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs';
import { DataTable, editableTableReducer } from '../../components/DataTable';
import { users } from '../data';

<Meta title="Library/DataTable" component={DataTable} />

# Editable Datatable

This is an example of an editable table.

<Canvas>
  <Story name="Editable example">
      {() => {
      const { Column, ColumnSelector, StickyHeader } = DataTable;
      const onSave = (data) => {
        console.log("To API: ", data)
      }
      return (
          <DataTable data={users(10)} reducers={{ editableTableReducer }} onSave={() => toApi}>
            <Column id="id">Uuid</Column>
            <Column id="firstname">First name</Column>
            <Column id="lastname">Last name</Column>
            <Column id="email">Email</Column>
            <Column id="title">Title</Column>
            <Column id="country">Country</Column>
        </DataTable>
      )
    }}
  </Story>
</Canvas>
