import { Meta, Story, Canvas, ArgsTable, Source } from '@storybook/addon-docs';
import { Dialog } from '../../components/Dialog';

<Meta title="Library/Dialog/Documentation" />

# Dialog

The `<Dialog>` comes with these props:

<ArgsTable of={Dialog} />
